{
  "name": "@task-cli/core",
  "version": "1.0.0",
  "description": "Shared types and business rules for task-cli (CLI, server, and web).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
